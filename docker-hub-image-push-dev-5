pipeline {
    agent { label 'dev-env' } 
    
    stages {
        stage('Push image to Hub') {
            steps {
                withCredentials([string(credentialsId: 'dockerhub-pwd', variable: 'dockerhubpwd')]) {
                    script {
                        sh "docker login -u rohitp779 -p ${dockerhubpwd}"
                        sh "docker tag tomcat:9 rohitp779/devops-track:latest" 
                        sh "docker push rohitp779/devops-track:latest"
                    }
                }
            }
        }
    }
}
